# ORCHESTRATION PLAN — 本家Minecraft忠実化 最終ロードマップ

作成: 2026-07-11（Fable 5 司令塔設計）

> **実際の作業はこのファイルではなく `計画書/` フォルダを使うこと。**
> `計画書/00_使い方.md` から始め、番号順の手順書（01〜22）をコピペで発注する。
> 各手順書は本ファイルから生成された自己完結版（モデル指定・検収チェック・品質未達時の対処つき）。
> 本ファイルは設計判断のマスター資料として残す（迷ったら §0-3 が最終判断基準）。

進め方: 1タスク=1セッション=1コミット=1push（[[commit-push-style]] 準拠でドキュメント更新も同コミットに同梱）。

---

## 0. 全体方針

### 0-1. ゴール定義

「本家Minecraftの機能をすべて」を無条件に追うと完成しないため、ゴールを3層で定義する。

- **ゴールA（必達）: 本家の見た目と手触り** — ライティング（暗い洞窟・松明の光・スムースライティング/AO）、流れる水/溶岩、泳ぎ/溺れ、階段/ハーフブロック/はしご、スニーク/スプリント。ここが埋まると「Minecraftに見える/Minecraftの操作感になる」。
- **ゴールB（必達）: サバイバルの進行ループ完成** — XP→エンチャント/金床→黒曜石→ネザー→醸造→エンド→ドラゴン討伐（クリア目標）。本家の「ゲームとして最後まで遊べる」構造。
- **ゴールC（品質）: コンテンツの厚み** — クモ/エンダーマン、革/骨粉/糸、調理と満腹度、羊毛/本棚/砂利、金ツール、レッドストーン基礎、ワールド選択画面。

**明示的に対象外（実装しない）**: マルチプレイ、コマンド/データパック互換、全バイオーム/全ブロックの網羅、ピストンを含む完全レッドストーン、村の襲撃イベント。ブラウザ1人用クローンとして費用対効果が合わないため。

### 0-2. 役割分担

- **Codex（または Opus）**: 実装全般（C1〜C18）。ロジック、物理、描画、ワーカー、セーブ。
- **Sonnet**: ユーザー向け文章（S1〜S4）。UI文言、ヘルプ、README、進捗テキスト、名称統一。DOM/CSSの軽い実装まで可。
- **ユーザー**: 各タスク完了時のレビュー（§5のチェックリストを使用）とpush判断。

### 0-2b. タスク別モデル割り当て（推論設定込み）

タスクの難度は3段階。**上位モデルの利用枠（サブスク残量）は難度「重」に温存する**こと。

| 難度 | タスク | 第一候補 | 代替 | 設定 |
|---|---|---|---|---|
| **重**（アーキテクチャ級。ワーカー/メイン二重実装・性能制約・ディメンション切替が絡む） | C4, C5, C6, C8, C16, C17 | Claude **Opus 4.8** | Codex の最上位モデル | 拡張思考/Reasoning は**最高**に。1タスク1セッション厳守 |
| **中**（複数ファイルにまたがる標準実装） | C1, C2, C9, C10, C11, C12, C15, C18 | **Codex**（標準モデル） | Claude Sonnet 5 | Reasoning は中〜高。既存パターン（ドアID方式・コンテナUI等）の踏襲を指示文で強調済み |
| **軽**（既存フローへの追記が中心） | C3, C7, C13, C14 | **Codex**（標準〜軽量） | Claude Sonnet 5 | 既定設定でよい |
| 文言 | S1〜S4 | Claude **Sonnet 5** | — | 実装コード変更禁止の縛りがあるため軽量モデルで十分。ただしS2(README)は裏取りにコードを読むのでHaikuまでは下げない |

運用ルール:
1. **格上げルール**: 同じタスクを同じモデルで2回失敗（受け入れ条件未達 or 回帰発生）したら、1段上のモデルに切り替えて再発注する。逆に「軽」タスクをOpusでやるのは枠の浪費なので避ける。
2. **1タスク=1新規セッション**。前タスクの文脈を引きずらせない（指示書は自己完結している）。
3. **レビュー補助（任意）**: 「重」タスクの検収前に、実装したのと**別の**モデル（例: Codex実装ならOpus、Opus実装ならCodex）に `git diff` を渡して「§3の該当タスクの受け入れ条件と禁止事項に照らしてレビュー」させると、ユーザー検収の負担が減る。
4. Claude Code 内でのモデル切替は `/model`。Opus 4.8 の Fast モードは高速化であって性能低下ではないが、「重」タスクでは通常モード+拡張思考を推奨。

### 0-3. アーキテクチャ上の決定事項（司令塔判断・変更禁止）

1. **ブロックの向き/状態は「IDバリアント方式」を継続する。** ドア16ID方式が実証済み。階段=4向き×素材、ハーフ=上下×素材もIDで表現する。座標メタデータ化の全面リファクタは行わない（工数対効果が合わない）。
2. **ライティングは「頂点カラー焼き込み方式」で実装する。** チャンクごとに skylight/blocklight を BFS 伝播した LightMap を作り、メッシュ頂点カラーに焼く。Three.js の動的ライトを増やす方式は禁止（性能が破綻する）。
3. **流体はレベル値(0-7)のセルオートマトン + 更新キュー方式。** 毎フレーム全走査は禁止。
4. **ネザー/エンドは「別ワールドウィンドウ」ではなく「別シード+別地形関数への切替」として実装する。** 既存の `regenWindow`/チャンク機構を使い回す。
5. **セーブ互換を壊さない。** `mc_save_<seed>` に新フィールドを追加する場合は必ずデフォルト値でフォールバックする。既存セーブが読めなくなる変更は禁止。
6. **メッシュ出力が変わるタスクは `MESH_WORKER_VERSION` を+1する**（IndexedDBキャッシュの世代分離）。現在値はソース内を検索して確認すること。

### 0-4. 本家との差分解析サマリ（優先度根拠）

| 領域 | 現状 | 本家 | 差の大きさ | 対応タスク |
|---|---|---|---|---|
| ライティング | Phongライト+固定環境光。洞窟も夜もフラットに明るい | ブロック光0-15伝播、スムースライティング、AO | **最大（見た目の正体）** | C4-C6 |
| 流体 | 静的1マス水源のみ | 流れる水/溶岩、無限水源、黒曜石/丸石生成 | 大 | C8, C10の一部 |
| 水中 | 物理なし（水中でも通常移動・無呼吸） | 泳ぎ、浮力、酸素、溺れ、青霧 | 大 | C9 |
| 移動 | 歩き/走り/ジャンプのみ | スニーク（落下防止）、スプリント（空腹消費・FOV）、はしご | 中 | C2, C3 |
| 建材 | 立方体+ドア/フェンス系のみ | 階段、ハーフ、板ガラス、看板、羊毛… | 中 | C1, C2, C14 |
| 成長システム | なし | XP→エンチャント→金床 | 大（B必達） | C10, C11 |
| 敵の生態 | 4種、簡易スポーン | クモ/エンダーマン等、光量スポーン | 中 | C7, C12 |
| 食料 | 生食のみ・回復固定 | 調理、食料別回復値、隠し満腹度 | 小 | C13 |
| ディメンション | なし | ネザー（醸造素材）、エンド（クリア） | 大（B必達） | C16, C17 |
| レッドストーン | なし | 回路全般 | 大（基礎のみやる） | C15 |
| ワールド管理 | シード指定のみ | 複数ワールド、タイトル画面 | 中 | C18, S1 |

---

## 1. 実行役に必ず添える共通前提（毎回、指示の先頭に貼ること）

```
【共通前提 — このプロジェクトの鉄則】
- リポジトリ: Vite + Three.js のボクセルゲーム。ソースは src/game/parts/*.js（番号順に結合され
  src/game.generated.js が生成される。生成物は絶対に手で編集しない。npm run assemble で再生成）。
- 関数宣言はモジュール全体でホイストされるためファイル番号をまたいだ呼び出しは可。ただし
  トップレベル実行時の変数参照は定義順（番号順）に注意。
- 検証コマンド: npm.cmd run check（assemble+vite build）。これが通ることは最低条件で、十分条件ではない。
  必ず npm.cmd run dev のプレビューを実ブラウザで開き、機能を操作して確認する。
  プレビュータブがバックグラウンドだと rAF がスロットルされ時間系が止まって見えるので、計測は
  フォアグラウンドで行う。window.__mcDbg（82-weather-and-loop.js 末尾）にテスト用フックが多数ある。
  新機能には必ず __mcDbg にテストフックを追加し、その使い方を成果報告に書くこと。
- ブロック追加の定番フロー: 20-textures.js にCanvasテクスチャ（+末尾normalMapリスト）→
  22-block-types.js に TYPES エントリと const → 53-inventory.js の ITEM_DEFS →
  55-crafting.js の RECIPES → 52-raycast.js（硬さ BLOCK_HARDNESS / 適正ツール blockPreferredTool /
  ツール制限 requiredToolTier / ドロップ blockDrops）。
- 非立方体形状は TYPES[type].model（{box:[x0,y0,z0,x1,y1,z1]} / {kind:'cross',...}）。
  当たり判定は collisionBoxes / collisionHeight。メッシュ描画はメインスレッド(34-mesh-rebuild.js)と
  Web Worker(src/world-mesh-worker.js)の両方に同じ結果が出るよう実装し、
  メッシュ出力が変わったら 34-mesh-rebuild.js の MESH_WORKER_VERSION を +1 する。
- ブロック編集の永続化は必ず setEdit() 経由（edits.set 直呼び禁止）。構造物生成は put/air 経由
  （world.set 直呼び禁止）。セーブは 33-save-state.js の統合 SAVE（mc_save_<seed>）。
  新フィールドは必ず「無ければデフォルト」で読み、既存セーブを壊さない。
- 【絶対禁止】60-rave-venues.js / 70-music.js（音楽会場）、music/、gikopoi2/、hallucinate/ の変更。
  英字キー R T Y U I O P [ Z X C V J K L M（会場中のみのものを含む）と N, G, F3-F11, 0 の
  既存キーバインドを奪わない。npmパッケージの追加も禁止（three固定）。
- 完了条件（全タスク共通）: ① npm.cmd run check 成功 ② プレビューで console error/warn ゼロ
  ③ 受け入れ条件をすべて実測 ④ 既存機能の回帰なし（最低限: 新規ワールド開始→採掘→設置→
  クラフト→敵と戦闘→セーブ→リロード復元 の一連が正常） ⑤ HANDOFF.md の先頭「現在地」を更新し、
  実装内容・触ったファイル・検証結果・新しい __mcDbg フックを追記（同一コミットに含める）。
```

---

## 2. タスク分解（依存関係つき）

### フェーズ0: バグ修正（最優先）
| ID | タスク | 依存 | 規模 |
|---|---|---|---|
| C19 | 岩盤・深層岩・地下メッシュ走査範囲の修正（掘り抜けて奈落落下するバグ。詳細は `計画書/23_C19_岩盤と地下描画の修正.md` が正） | なし | 中 |

### フェーズ1: 見た目と手触り（ゴールA）
| ID | タスク | 依存 | 規模 |
|---|---|---|---|
| C1 | 階段・ハーフブロック（IDバリアント+設置向き+衝突） | なし | 中 |
| C2 | はしご・板ガラス・看板（テキスト保存） | なし | 中 |
| ~~C3~~ | ~~スニーク・スプリント・FOV演出~~ **中止**: Shift=ダッシュ維持・スニーク不要（2026-07-12 ユーザー決定） | — | — |
| C4 | ライトエンジン①: skylight伝播+頂点カラー焼き込み | なし | **大** |
| C5 | ライトエンジン②: blocklight（松明等の光伝播・増分更新） | C4 | 大 |
| C6 | ライトエンジン③: スムースライティング+AO | C4 | 中 |
| C7 | 光量ベースのモブスポーン/日光燃焼への切替 | C5 | 小 |

### フェーズ2: 世界のシミュレーション
| ID | タスク | 依存 | 規模 |
|---|---|---|---|
| C8 | 流れる水/溶岩（レベル0-7オートマトン、無限水源、黒曜石/丸石） | なし(C4推奨後) | **大** |
| C9 | 水中物理（泳ぎ・浮力・酸素・溺れ・青霧・水中採掘減速） | C8 | 中 |

### フェーズ3: 進行ループ（ゴールB）
| ID | タスク | 依存 | 規模 |
|---|---|---|---|
| C10 | XPシステム（オーブ・レベル・取得源・死亡ロス）+黒曜石/火打石と打ち金 | なし | 中 |
| C11 | エンチャントテーブル+金床（簡易エンチャント・修理・合成） | C10 | 中 |
| C12 | 追加モブ: クモ・エンダーマン簡易＋革/骨粉/糸/腐肉ドロップ整合 | C7推奨 | 中 |
| C13 | 調理と満腹度（焼肉、食料別回復値、隠し満腹度、金リンゴ） | なし | 小 |
| C14 | 建材/素材拡充（羊毛+染色基本、本棚、砂利+火打石、金ツール、氷） | なし | 中 |
| C16 | ネザー（ポータル・地形・ブレイズ簡易・要塞ミニ・醸造） | C10,C12 | **大** |
| C17 | エンド+エンダードラゴン（要塞探索→ポータル→ボス戦→クリア画面） | C16 | **大** |

### フェーズ4: 品質と外装
| ID | タスク | 依存 | 規模 |
|---|---|---|---|
| C15 | レッドストーン基礎（ダスト・トーチ・レバー/ボタン/感圧板・ランプ・ドア連動） | なし | 大 |
| C18 | ワールド選択/作成画面+複数ワールド管理 | なし | 中 |
| S1 | タイトル/ポーズ/ヘルプ画面の文言と軽量DOM | 随時 | 小 |
| S2 | README全面改稿 | 機能確定後 | 小 |
| S3 | 進捗ツリー拡張（新機能連動の目標/達成文） | C10-C17後 | 小 |
| S4 | ゲーム内文言監修（アイテム名統一・死亡メッセージ・ツールチップ） | 随時 | 小 |

---

## 3. Codexに渡す指示（コピペ用・各タスク完結）

> それぞれ §1 の【共通前提】を先頭に貼ってから渡すこと。

### C1: 階段とハーフブロック

```
【目的】本家の建築の基本である階段とハーフブロックを追加し、建築表現と移動導線を本家に近づける。

【対象ファイル】20-textures.js / 22-block-types.js / 53-inventory.js / 55-crafting.js /
52-raycast.js / 34-mesh-rebuild.js / src/world-mesh-worker.js / 50-player-physics.js（確認のみ）

【仕様】
1. 階段: オーク(板材)・丸石・石レンガの3素材 × 4向き = 12個のIDバリアント。既存ドアの
   IDバリアント方式（22-block-types.js の setDoorShape 参照）に倣う。形状は model 2箱
   （下半分フル + 上半分半奥行き）、collisionBoxes も同じ2箱。アイテムは素材ごとに1つ
   （oak_stairs / cobblestone_stairs / stone_brick_stairs）で、設置時にプレイヤーの視線方位から
   向きIDを決める（ドアの設置ロジック参照）。どの向きを壊しても素材の階段1個をドロップ。
2. ハーフブロック: 同3素材 × 上付き/下付き = 6ID。アイテムは素材ごと1つ。設置時、クリックした
   面が上面なら下付き、下面なら上付き、側面ならクリック位置の高さで判定（ヒット点のy小数 >= 0.5
   で上付き）。model/collisionBoxes は半箱。
3. レシピ: 階段=素材6個の階段形 ['M  ','MM ','MMM'] → 4個。ハーフ=素材3個 ['MMM'] → 6個。
   キーは素材ごと（PLANKS/COBBLESTONE/STONE_BRICK）。
4. 採掘: 硬さ・適正ツールは元素材と同じ。木系は斧、石系はツルハシ。
5. 既存の1.05ステップアップ物理で0.5段は自動で登れるはず。階段を2段並べて歩いて登れることを確認。

【受け入れ条件】
- 3素材の階段が視線に応じた向きで設置され、階段2連で段差2を歩いて登れる
- ハーフブロックが上付き/下付きを打ち分けられ、下付きハーフの上に立てる（高さ0.5）
- 全レシピが作業台で一致し、壊すと正しいアイテムがドロップ、リロード後も形状が復元される
- メッシュはワーカー経路とフォールバック経路で同一に見える（MESH_WORKER_VERSION を+1すること）

【テスト方法】
- __mcDbg に give('oak_stairs') 等で入手→4方位から設置してスクショ確認
- __mcDbg.tryRecipe で全6レシピの一致を確認
- セーブ→リロードで向きが保存されることを確認

【禁止事項】共通前提の禁止事項に加え、既存ドア/フェンスのID(50-73)の並びを変えないこと。
向きの座標メタデータ化に手を出さないこと（IDバリアント方式で完結させる）。
```

### C2: はしご・板ガラス・看板

```
【目的】縦移動（はしご）と建築ディテール（板ガラス・文字が書ける看板）を追加する。

【対象ファイル】20-textures.js / 22-block-types.js / 53-inventory.js / 55-crafting.js /
52-raycast.js / 50-player-physics.js / 82-weather-and-loop.js / 33-save-state.js / index.html or styles.css

【仕様】
1. はしご: 4向きID（壁の4面に貼る薄板 model、厚さ0.125）。レシピ=棒7個のH字['S S','SSS','S S']→3個。
   設置は壁面クリック時のみ（対象面が側面のとき）。物理: プレイヤーAABBがはしごブロックと重なって
   いる間、重力を殺し、前進キーまたはジャンプで上昇(約2.4/s)、スニークで停止、下キーなしなら
   ゆっくり滑降(約1.5/s)。落下ダメージはしご接触でリセット。
2. 板ガラス: フェンスと同様の静的十字モデル（中央板2枚direction交差、厚さ0.125、collisionBoxes同形）。
   レシピ=ガラス6個['GGG','GGG']→16個。壊すと本家同様ドロップなし（ガラス同様）にするか迷う場合は
   ドロップなしを採用。
3. 看板: 立て看板のみ（既存 VILLAGE_SIGN の形状を流用可）。4向きID。レシピ=板材6+棒1→3個。
   設置直後にポインタを解放して小さな入力ダイアログ（1行×最大4行テキスト）を出し、確定で
   SAVE.signs["x,y,z"]=テキスト として保存。表示は CanvasTexture を板面に貼る（座標ごとに生成、
   dispose 管理）。壊すと看板アイテムをドロップしテキストを削除。右クリックで再編集。
4. 入力ダイアログは releasePointerForUi()/relockPointerForGame()（54-input.js）を使い、
   閉じたらポインタロックへ戻す。

【受け入れ条件】
- はしごで垂直の壁を登り降りでき、途中で手を離すとゆっくり滑り、着地で落下ダメージなし
- 板ガラスが窓として1ブロック厚未満に見える。フェンス同様の当たり判定
- 看板に日本語テキストを書き、表示され、リロード後も残り、再編集・破壊削除ができる
- MESH_WORKER_VERSION +1、全レシピ一致、console error/warn ゼロ

【テスト方法】__mcDbg.give で各アイテム入手→実操作。SAVE.signs の保存/復元を localStorage で確認。
はしご物理は高さ10の柱を作って登降とHP無変化を確認。

【禁止事項】共通前提に同じ。看板テキストのHTML挿入に注意（textContent/Canvas描画のみ、innerHTML禁止）。
```

### C3: スニーク・スプリント・FOV演出

```
【目的】本家の移動の手触り（スニークで縁から落ちない、スプリントで視野が広がり空腹が減る）を再現する。

【対象ファイル】50-player-physics.js / 54-input.js / 51-survival.js / 82-weather-and-loop.js（ループ配線）

【仕様】
1. スニーク: ShiftLeft 押下中、速度 1.31/s、目線を0.15下げ、【重要】onGround かつスニーク中は
   移動後の足元にブロックが無くなる移動を打ち消す（本家の「縁で止まる」挙動）。moveAxis で
   移動先の (x,z) 直下1.0以内に衝突ボックスが無ければその軸移動をキャンセルする実装でよい。
2. スプリント: W ダブルタップ（0.3秒以内）または Ctrl+W で開始。前進が途切れる/空腹が6以下で解除。
   速度は既存 SPRINT 定数を使用。スプリント中は camera.fov を +10 へ 0.15秒でイージング（解除で戻す。
   camera.updateProjectionMatrix() を忘れない）。空腹消費: スプリント100mごと相当で0.5、
   ジャンプ・採掘・戦闘にも微小な消費を追加（詳細な数値はコード内定数にまとめ調整可能に）。
3. 空腹回復: 空腹18以上でHPが2秒ごとに0.5自然回復（既存仕様と重複するなら整合させる）。
4. Shift はインベントリの Shiftクリック（一括移動）と競合しないこと（コンテナ開時は移動系を止める
   既存ガードを確認）。

【受け入れ条件】
- スニークで1ブロックの縁を歩いても落ちない。スニーク解除で落ちる
- Wダブルタップでスプリント開始、FOVが滑らかに広がる、空腹が徐々に減る、空腹6以下で走れない
- 既存の会場ダンスキー(Z/X/C/V)や設定キーと競合しない

【テスト方法】高さ5の柱の上でスニーク移動テスト。__mcDbg に survival() で空腹値を監視しつつ
スプリント。fov の値を preview の javascript 評価で確認。

【禁止事項】共通前提に同じ。WALK/SPRINT/GRAVITY/JUMP の既存定数値を変えない（手触り回帰防止）。
```

### C4: ライトエンジン①（skylight + 頂点カラー）

```
【目的】本家の見た目の核であるライトエンジンの第1段。空からの光(skylight 0-15)をチャンクごとに
計算し、メッシュ頂点カラーに焼き込む。これにより「洞窟や屋内が本当に暗い」世界にする。

【対象ファイル】src/world-mesh-worker.js / 34-mesh-rebuild.js / 22-block-types.js(mkMat) /
82-weather-and-loop.js / 12-lights.js（既存ライトの役割整理）

【仕様】
1. ワーカーの buildChunkState 後段に LightMap 計算を追加: 対象チャンク±1（パディング1チャンク、
   既存 collectMeshPayload の3x3範囲と同じ）で、各列の最上solid以上を15とし、下方向は透過ブロック
   （transparent/非solid。水は-3減衰）で15維持…ではなく本家式に「垂直は減衰なしで15、
   横方向伝播はBFSで距離1につき-1」。配列は Uint8Array(チャンク幅+2)^2×高さ を列スタック
   (columnStack)から構築。高さ方向は実ブロックのある範囲+マージンのみでよい（メモリ節約）。
2. メッシュ生成時、各面の頂点カラーとして「その面が接する空気側セルの light 値」を採用し、
   geometry に color attribute (3成分、light/15 の輝度) を追加。マテリアル側は
   mkMat に vertexColors: true を全ブロックで有効化。
3. メインスレッド側: 昼夜サイクルはこれまで通り環境光で表現するが、skylight の頂点カラーは
   「空がどれだけ届くか」の係数として働く（洞窟=0で常に暗い、地上=15で昼夜どおり）。
   82 の updateWeather/updateDayNight の環境光・ライト強度を再調整し、
   ・昼の地上は現状と同程度の明るさ
   ・洞窟(skylight 0, blocklightなし)はほぼ真っ暗（松明が必須になる暗さ、ただし完全な0ではなく0.03程度）
   になるようにする。夜の地上は skylight があっても暗い（環境光を下げる）。
4. 頂点カラーの最小値: このタスクの時点では blocklight が無いので、既存の PLACED_LIGHTS
   （プレイヤー設置の松明等の Three.js PointLight）はそのまま残す（C5で置換する）。
5. フォールバック経路（メインスレッドの同期ビルド）にも同じ計算を実装し、結果が一致すること。
6. MESH_WORKER_VERSION +1。パフォーマンス: 1チャンクのライト計算込みビルドが 60ms を超えないこと
   （現状37ms。BFSは荒く実装すると爆発するので、伝播は「空気セルかつ light>1」のみキュー投入）。

【受け入れ条件】
- 洞窟の奥が本当に暗く、入口からの光のグラデーションが見える（スクショ添付）
- 地上の昼の明るさ・夜の暗さが従来と同等以上の見栄え
- 家を建てて屋根を付けると屋内が暗くなり、窓から光が差す
- Node ベンチ（HANDOFF記載の bench.mjs 方式）で1チャンク60ms以内
- errors=0 / fallbacks=0、キャッシュ再訪問でも正しい明るさ

【テスト方法】__mcDbg に lightAt(x,y,z) フックを追加（ワーカーと同ロジックのデバッグ実装でよい）。
F4で洞窟へ→内部スクショ。地上に3x3x3の小屋を建て屋内の暗さを確認。

【禁止事項】共通前提に同じ。Three.js の PointLight/SpotLight を新たに追加しない。
チャンクあたりのフル3D配列(384高さ全部)を常時確保しない（メモリ）。
```

### C5: ライトエンジン②（blocklight・増分更新）

```
【目的】松明・ランタン・溶岩などの発光ブロックから光をBFS伝播させ、本家の「松明で照らす」体験を
完成させる。動的PointLight方式(12-lights.js/PLACED_LIGHTS)からライトマップ方式へ置き換える。

【対象ファイル】src/world-mesh-worker.js / 34-mesh-rebuild.js / 12-lights.js / 52-raycast.js /
22-block-types.js / 82-weather-and-loop.js

【仕様】
1. 発光ブロック定義: TYPES に lightLevel を追加（松明14、ランタン15、提灯14、発光結晶13、
   溶岩15、かまど(点火)13、TNT?なし、ポータル系は後続タスク）。ワーカーへのペイロード
   blockModels と同様に lightLevels 表を渡す。
2. skylight(C4)と同じ配列に blocklight チャンネルを追加（別Uint8Arrayまたは上位4bit/下位4bitパック）。
   光源セルに lightLevel を置き、全方向BFSで-1減衰。頂点カラー = max(skylight係数×昼夜, blocklight)
   になるようシェーダ入力を設計（実装簡略化のため、頂点カラーを2アトリビュート(sky,block)にし、
   mkMat の onBeforeCompile で `light = max(sky * dayFactor, block)` を計算する。dayFactor は
   uniform で毎フレーム更新）。onBeforeCompile が複雑すぎる場合の代替: 頂点カラー1本に
   max(sky×現在昼夜, block) を焼き、昼夜変化時に「見えているチャンクを数秒かけて順次再メッシュ」
   でもよいが、性能とちらつきに注意（推奨は uniform 方式）。
3. 増分更新: ブロック設置/破壊時、影響半径15ブロック内のチャンク（最大3x3）を再メッシュキューへ。
   既存の requestEditedBlockRebuild の仕組みを使う。ライトは再計算でよい（差分伝播までは不要）。
4. 12-lights.js の PLACED_LIGHTS の PointLight 生成は廃止し、互換のためレジストリだけ残す
   （スポーン抑制がC7で light 値参照に切り替わるまで既存ロジックを壊さない）。
5. MESH_WORKER_VERSION +1。

【受け入れ条件】
- 真っ暗な洞窟に松明を置くと半径14の光が距離減衰つきで広がる（スクショ）
- 松明を壊すと周囲が暗く戻る（数フレーム以内）
- 夜の地上で松明・ランタン・かまどの明かりが本家らしく見える
- 昼夜の移り変わりで屋外は暗く/明るくなるが、松明の光は一定
- FPSが従来から大きく落ちない（高速飛行時のフレーム時間を HANDOFF の計測法で確認）

【テスト方法】F4で洞窟→松明設置/破壊。夜(setTime)の村の明かりを確認。
__mcDbg.lightAt を blocklight 対応に拡張。

【禁止事項】共通前提に同じ。動的PointLightの追加禁止。既存の太陽光/環境光の全体設計を壊さない。
```

### C6: ライトエンジン③（スムースライティング + AO）

```
【目的】頂点ごとの光の平均化と角の陰(Ambient Occlusion)で、本家の柔らかい陰影を再現する。
これが入ると画面が一気に「Minecraft」になる。

【対象ファイル】src/world-mesh-worker.js / 34-mesh-rebuild.js

【仕様】
1. 各面の4頂点それぞれについて、面の空気側にある「頂点を共有する4セル」の光値
   （sky/block 各チャンネル）を平均する（本家のスムースライティング）。
2. AO: 頂点に隣接する side1/side2/corner の solid 判定で 0-3 段階の遮蔽を計算し
   （Minecraft AO の標準アルゴリズム: side1&&side2 なら3）、光値に係数
   [1.0, 0.8, 0.65, 0.5] を乗じる。
3. 四角形の対角分割方向を AO 値に応じて flip する（anisotropy 対策の定番）。
4. モデルつきブロック（ドア・フェンス等の box パーツ）は簡易でよい: パーツ面はそのセルの
   光値をフラットに使う（AOなし）。
5. MESH_WORKER_VERSION +1。ビルド時間の増分は+30%以内に収める。

【受け入れ条件】
- ブロックの接地際・壁の入隅に柔らかい陰が出る（本家スクショと並べて比較して違和感がない）
- 平地のチェッカーボード状のアーティファクトが出ない（対角flipの確認）
- 洞窟内の松明光のグラデーションが頂点単位で滑らか

【テスト方法】平原+建物+洞窟の3箇所でスクショを撮り本家の画像と見比べる。ベンチで時間確認。

【禁止事項】共通前提に同じ。スクリーンスペースAO(ポストプロセス)で代替しない（[[minecraft-web-next-steps]]
でシェーダーパック方針はキャンセル済み。あくまでボクセル頂点AO）。
```

### C7: 光量ベースのモブスポーン

```
【目的】敵の湧き条件を本家同様「ブロックの光量」に統一する（暗ければ地上の屋内でも湧く、
明るければ洞窟でも湧かない）。

【対象ファイル】39-hostile-mobs.js / 51-survival.js（ゾンビ日光燃焼まわりが52/39どちらにあるか確認）

【仕様】
1. trySpawnMobs の湧き判定を「候補セルの blocklight <= 0 かつ (skylight <= 7 または夜)」へ変更。
   メインスレッドから光値を得る手段が必要: C4/C5 でワーカーが返す LightMap をチャンクごとに
   メイン側で保持し、lightAt(x,y,z) を提供する（無ければ保守的に湧かない）。
2. PLACED_LIGHTS 半径方式の抑制ロジックと isCoveredFromSky は光量判定へ置換して削除
   （日光燃焼だけは skylight>=14 かつ昼、へ置換）。12-lights.js の不要コードを掃除。
3. スポーン上限・間隔・距離の既存バランス値は維持。

【受け入れ条件】
- 夜、松明で照らした自宅周辺に敵が湧かない／照らしていない暗い屋内には昼でも湧く
- 洞窟で松明を並べると湧きが止まる
- 朝、日なたのゾンビ/スケルトンだけが燃える（日陰・屋内は燃えない）

【テスト方法】__mcDbg.spawn/setTime/mobs を使用。屋内暗室(光0)を作って昼スポーンを確認。

【禁止事項】共通前提に同じ。スポーンレート定数を変えない。
```

### C8: 流れる水と溶岩

```
【目的】静的な液体を本家式の「流れる液体」にする。バケツ・地形の水・溶岩すべてが対象。

【対象ファイル】22-block-types.js / 32-world-window.js / 52-raycast.js / 34-mesh-rebuild.js /
src/world-mesh-worker.js / 82-weather-and-loop.js / 33-save-state.js / 51-survival.js

【仕様】
1. 液体表現: WATER/LAVA を「ソース」とし、流水はレベル1-7を別ID帯 or レベル配列で表現する。
   推奨実装: 新Map `liquids`（"x,y,z" -> {type:'water'|'lava', level:0-7}）+ チャンク索引。
   level 0 = ソース。メッシュはレベルに応じて上面の高さを (8-level)/9 に下げる
   （ワーカーへ liquids を payload で渡す。フォールバック経路も同様）。
2. シミュレーション: 更新キュー方式。液体セルの設置/破壊/隣接変化でキューに積み、
   tick(水=0.25s間隔, 溶岩=1.0s間隔)ごとに処理:
   - 下が空気なら下へ流れる(下は level 1 の落下水)。下が塞がれていれば水平4方向へ level+1 で拡散
     (水は最大7、溶岩は最大4)。
   - 供給元が消えたら再計算して枯れる(隣接の level-1 供給が無ければ自分も消える)。
   - 無限水源: 水ソースが水平に2つ隣接する空気セル(下が solid)はソース化。
   - 水×溶岩: 溶岩ソース+水接触=黒曜石、流溶岩+水=丸石。石化時に「ジュッ」音(44-sound-effects.js)。
3. 地形生成の海/川/湖の水は従来どおり暗黙ブロックでよい(全部をシムに載せない)。シム対象は
   「プレイヤーが置いた液体」「採掘等で隣接が変化した液体」だけに限定する(性能維持のため)。
   セーブ: liquids を SAVE に保存(ソースのみ保存し、流水はロード時に再シムでよい)。
4. 溶岩接触ダメージ・炎上(2秒)を追加。水に入ると炎上解除。
5. プレイヤー/アイテムドロップは流れに押される(レベル勾配から流向ベクトルを計算、軽く加速)。

【受け入れ条件】
- バケツで置いた水が階段状に7ブロック広がり、崖で滝になる。汲む/塞ぐと枯れる
- 2x2の穴に水2つで無限水源ができる
- 溶岩に水をかけると黒曜石/丸石ができる。溶岩で炎上し、水で消える
- 放置してもFPSが落ちない(アイドル時はキュー空でコスト0であることを確認)
- セーブ→リロードで水地形が復元される

【テスト方法】__mcDbg に liquidInfo(x,y,z)/simStats() を追加。滝・無限水源・黒曜石生成を実測。
性能は5分放置でフレーム時間が変わらないこと。

【禁止事項】共通前提に同じ。毎フレーム全液体走査の実装は禁止(必ずイベント駆動キュー)。
自然地形の海・川を初期状態で全部シムに載せない。
```

### C9: 水中物理

```
【目的】泳ぎ・浮力・酸素・溺れ・水中視界を追加し、水を「入れる場所」にする。

【対象ファイル】50-player-physics.js / 51-survival.js / 82-weather-and-loop.js / styles.css or HUD /
44-sound-effects.js

【仕様】
1. プレイヤーAABBの目線/胴体が水中かの判定を毎フレーム実施(blockAt が WATER または liquids の水)。
2. 水中物理: 重力を約1/4、落下速度上限-2.2、Space長押しで上昇(浮上1.9/s)、移動速度は歩きの約0.5倍
   (スプリント泳ぎは1.5倍まで)。水面で頭が出たらジャンプで小さく跳ねられる。落下ダメージは
   着水でリセット。
3. 酸素: 頭(目線)が水中の間、10ゲージ(バブル)を15秒で消費。0になったら1秒ごとに2ダメージ。
   頭が出れば即全回復。HUDにバブル表示(空腹バーの上、頭が水中の時のみ表示)。
4. 視界: 頭が水中の間、霧を青(#1a4a8a近辺)・near/farを短く、環境音をこもらせる(BGM音量-30%)。
5. 水中の採掘速度を1/5に(既存の採掘速度計算に係数)。

【受け入れ条件】
- 深い水に飛び込む→沈む→Spaceで浮上→水面で息継ぎ、の一連が本家の感覚
- 水底に15秒潜ると溺れダメージが始まり、浮上で止まる。バブルHUDが減る/回復する
- 水中で画面が青くなり、出ると戻る。落下ダメージが水で無効化される

【テスト方法】F9の地下湖 or 自作プールで実測。__mcDbg.survival() で酸素値を出す。

【禁止事項】共通前提に同じ。EYE/HALF等の体格定数を変えない。
```

### C10: XPシステム + 黒曜石/火打石と打ち金

```
【目的】本家の成長リソースであるXPを導入し、あわせてネザーへの道具(黒曜石採掘・着火)を揃える。

【対象ファイル】39-hostile-mobs.js / 41-item-drops.js(参考) / 51-survival.js / 52-raycast.js /
53-inventory.js / 55-crafting.js / 20-textures.js / 22-block-types.js / 33-save-state.js / HUD(styles.css)

【仕様】
1. XPオーブ: 新エンティティ(小さな緑発光スプライト、既存 ITEM_DROPS の実装を雛形に別リスト XP_ORBS)。
   吸引半径2.4/取得1.1は同じ。取得で xp 加算+チン音。
2. 取得源: 敵討伐(ゾンビ/スケルトン/クリーパー5、スライム2-4)、鉱石採掘(石炭1-2、ダイヤ/金3-7、
   ※シルクタッチ無いので常時)、精錬取り出し(0.5-1相当を蓄積)、繁殖1-3。
3. レベル: 本家式の必要量テーブル(Lv0-15: 2L+7 …)を簡略化してよいが単調増加にする。
   HUDにレベル数字+緑バー(ホットバー直上)。SAVE.xp {level, points} を保存。
4. 死亡時: XPは失わない(このゲームはユーザー方針で「キープインベントリ」= 死んでもアイテム・XPを
   ロストしない。51-survival.js の diePlayer 参照。本家の死亡ロストは実装しないこと)。
5. 黒曜石: 新ブロック(硬さ大、ダイヤツルハシ必須 requiredToolTier=4、採掘10秒級)。C8の生成に加え
   地形の溶岩湖表面にも露出させられれば尚可(任意)。
6. 火打石と打ち金: レシピ=鉄インゴット+火打石(火打石はC14の砂利から。C14未実装の間は
   ITEM_DEFS に火打石を先行追加し、砂利ドロップは後続タスクで接続)。使うと対象面に炎…は
   実装コストが高いので「TNT着火」「ネザーポータル着火(C16)」「敵への着火」専用でよい。耐久65。

【受け入れ条件】
- 敵を倒す/鉱石を掘るとオーブが飛び散り、吸い寄せられ、バーが伸びてレベルアップ音が鳴る
- 死亡でオーブが散らばり、拾い直せる。リロードでレベル/ポイント復元
- 黒曜石がダイヤツルハシでのみ回収できる(鉄では壊せてもドロップしない)
- 火打石と打ち金でTNTに着火できる

【テスト方法】__mcDbg に xp()/giveXp(n) を追加。討伐/採掘/精錬/死亡の各経路を実測。

【禁止事項】共通前提に同じ。エンチャント(C11)には踏み込まない。
```

### C11: エンチャントテーブル + 金床

```
【目的】XPの使い道を作る。簡易エンチャントと、修理/合成の金床。

【対象ファイル】20-textures.js / 22-block-types.js / 53-inventory.js / 55-crafting.js /
56-inventory-panel.js / 52-raycast.js / 33-save-state.js

【仕様】
1. エンチャントテーブル: レシピ=本1(本=紙3+革1、紙=サトウキビ…が無いので簡略: 本棚/本はC14で
   追加する「本」を使う。C14未実装なら本アイテムを本タスクで先行追加、レシピ=板材3+革1→革は
   C12で牛から。依存が未実装の間は __mcDbg.give で検証)+ダイヤ2+黒曜石4。
   右クリックでUI(既存コンテナUIの雛形を流用): 対象ツール/武器/防具スロット+3択ボタン
   (コスト1/2/3レベル)。選択肢は簡易テーブルから抽選:
   効率I-III(採掘速度+30%/段)、耐久I-III(耐久消費を1/(1+段)確率でスキップ)、
   ダメージI-III(+1.25/段)、防護I-III(軽減+8%/段)、火属性I(攻撃で炎上)、無限I(矢を消費しない)。
   周囲の本棚数(半径2)で高コスト枠が解放される(0冊=Lv1のみ、15冊=Lv3まで)。
2. エンチャント効果の適用: アイテムに ench 配列を持たせ(スロットのitemオブジェクトに追加、
   セーブ互換に注意)、採掘速度(52)/戦闘(39)/防具軽減(51)/弓(39)の計算に反映。
   アイテム名表示に「効率 II」等を付記し、アイコンに紫の枠。
3. 金床: レシピ=鉄ブロック3+鉄インゴット4。UI: 素材2スロット+出力。同種ツール2つ→耐久合算+12%、
   またはツール+素材(鉄ツールに鉄等)で耐久50%回復。コスト=1-3レベル。エンチャントは
   高い方を引き継ぐ(競合は先勝ち)。
4. どちらのブロックも設置/破壊/ドロップ/硬さを既存フローで登録。

【受け入れ条件】
- 本棚で囲んだテーブルでLv3エンチャントが出る。効率IIIのダイヤツルハシで石の採掘が明確に速い
- 耐久/ダメージ/防護/火属性/無限が各系統で機能する(実測)
- 金床で耐久合成と修理ができ、エンチャントが引き継がれる
- エンチャント付きアイテムがセーブ/リロードで保持される。旧セーブも読める

【テスト方法】__mcDbg に enchant(slot, id, lvl) を追加して全効果を強制付与テスト。
UI経由の抽選も3回以上実測。

【禁止事項】共通前提に同じ。ラピスラズリ消費・エンチャント本・つるはし以外への幸運などは実装しない。
```

### C12: クモ・エンダーマン + ドロップ生態系整合

```
【目的】本家の代表モブを追加し、素材チェーン(糸・革・骨粉・エンダーパール)を完成させる。

【対象ファイル】39-hostile-mobs.js / 37-animals.js / 53-inventory.js / 55-crafting.js / 48-farming.js

【仕様】
1. クモ: 平たい体+8脚の簡易モデル(makeCreeper等の箱組み方式)。光量7以下で湧き、
   明るい所では中立(攻撃されたら反撃)。壁登り: 進行方向が壁なら垂直速度+3で登る。
   HP16、攻撃2、ドロップ=糸0-2。弓レシピの繊維を糸に変更…は既存互換を壊すので、
   レシピは「繊維 or 糸」の両対応キーにする。
2. エンダーマン(簡易): 夜の地上に低確率で湧く。長身の黒い箱モデル+紫の目。
   視線カーソルを合わせて0.5秒で敵対(本家式)、敵対中は3-5秒ごとにプレイヤー周囲へテレポート
   (紫パーティクル+音)。水/雨でダメージを受けて中立に戻る。HP40、攻撃4、
   ドロップ=エンダーパール0-1(C17で使用)。パール投擲は実装しない(C17のエンダーアイ専用)。
3. ドロップ整合: 牛→革0-2追加。ゾンビ→腐肉(食べると空腹4回復+30%で空腹ペナルティ…
   状態異常が無いので「回復量が低い非常食」でよい)。スケルトン→骨0-2、骨→骨粉3。
   骨粉: 作物/苗木に右クリックで成長を1段階進める(48-farming.js の growCrops/growSaplings を流用)。
4. スポーン抽選への組み込みは既存の重み方式。上限・距離バランスは変えない。

【受け入れ条件】
- クモが壁を登って追ってくる。昼の明所では襲ってこない
- エンダーマンを見つめると敵対し、テレポートで背後に回られる。倒すとパールが出る
- 骨粉で小麦が即実る。革・腐肉・骨・糸が正しくドロップし、既存レシピが壊れていない

【テスト方法】__mcDbg.mobKinds に追加し直接スポーン→AI/ドロップ実測。骨粉は farmTest と組み合わせ。

【禁止事項】共通前提に同じ。既存モブのスポーンバランス定数を変えない。
```

### C13: 調理と満腹度

```
【目的】食料に本家の深み(調理で価値が上がる、隠し満腹度)を足す。

【対象ファイル】53-inventory.js / 55-crafting.js / 51-survival.js / 52-raycast.js

【仕様】
1. かまど精錬に食料を追加(SMELT_RESULT): 生牛肉→ステーキ(空腹8)、生豚肉→焼き豚(8)、
   生鶏肉→焼き鳥(6)、じゃがいも等は無いので対象外。既存の生肉 ITEM_DEFS の id を確認して接続。
2. 全食料に hunger と saturation を定義(ITEM_DEFS拡張)。saturation は隠し満腹度:
   食べると hidden ゲージ(最大=現在の空腹値)に加算され、空腹減少はまず hidden から引かれる。
   本家の簡略版でよい。リンゴ4/2.4、パン5/6、ステーキ8/12.8、ベリー2/0.4、牛乳は現状維持。
3. 金リンゴ: 金インゴット8+リンゴ。食べると空腹4+吸収HP4(上限20を超える一時HP、
   被弾で先に減る。HUDでは黄色ハートで区別)。
4. 空腹18以上で自然回復(C3と重複するなら整合)、空腹0で飢餓ダメージは既存仕様を維持。

【受け入れ条件】
- 生肉を焼くと回復量が上がる。ステーキを食べると長時間空腹が減らない(saturationの実感)
- 金リンゴで黄色ハートが付き、被弾で先に消える
- 旧セーブの食料アイテムがそのまま使える

【テスト方法】__mcDbg.survival()/give で各食料の hunger/saturation 遷移を数値確認。

【禁止事項】共通前提に同じ。状態異常システム(毒等)には踏み込まない。
```

### C14: 建材/素材の拡充

```
【目的】建築と素材チェーンの穴を埋める: 羊毛、本/本棚、砂利/火打石、金ツール、氷。

【対象ファイル】20-textures.js / 22-block-types.js / 53-inventory.js / 55-crafting.js /
52-raycast.js / 32-world-window.js(砂利/氷の生成) / 37-animals.js(羊毛)

【仕様】
1. 羊毛: 羊のドロップ「布」を羊毛ブロックに改名・統合(白)。ハサミ(鉄2)で羊を右クリック→羊毛1-3、
   羊は刈られ状態(体色を肌色に)→90秒で回復。染色: 花から赤/黄染料(既存の花があるか36-plants.jsを
   確認、無ければ草原に赤黄の花を追加)、羊毛+染料→色羊毛(赤/黄/青の3色でよい。青は…ラピスが
   無いので花で代替)。ベッドレシピ(羊毛3+板材3)を追加し、既存のベッド入手手段と共存。
2. 本と本棚: 紙(サトウキビが無いので簡略: 小麦3→紙3)、本=紙3+革1、本棚=板材6+本3。
   本棚はC11のエンチャント強化に接続。壊すと本3ドロップ。
3. 砂利: 地形生成(川底・洞窟)にまばらに追加+ブロックとして登録。シャベル適正。
   ドロップ=10%で火打石、それ以外は砂利。C10の火打石と打ち金レシピに接続。
4. 金ツール: 金のツルハシ/斧/シャベル/剣。速度はダイヤ級だが耐久32、採掘ティアは木相当(本家準拠)。
5. 氷: 雪原バイオームの水面を氷に。半透明ブロック、壊すと水に戻る(ドロップなし)。上を歩くと滑る
   (摩擦低下、加速度を落とし慣性を残す)。

【受け入れ条件】
- ハサミで羊毛を刈れて羊が再生する。3色の羊毛とベッドがクラフトできる
- 本棚が作れてエンチャントLv3が解放される(C11連携)
- 砂利から火打石が出る。金ツールが高速で脆い。氷の上で滑る
- MESH_WORKER_VERSION +1(氷/砂利で地形が変わるため)

【テスト方法】各レシピ tryRecipe、羊毛刈り実測、雪原へ飛んで氷確認(テレポートフックがなければ
seed調整かF系キーで探す)。

【禁止事項】共通前提に同じ。16色フル染色には踏み込まない(3色まで)。
```

### C15: レッドストーン基礎

```
【目的】本家の象徴システムの基礎: 動力で「開く・光る・爆発する」を成立させる。

【対象ファイル】20-textures.js / 22-block-types.js / 53-inventory.js / 55-crafting.js /
52-raycast.js / 32-world-window.js(鉱石生成) / 新規 47-redstone.js / 82-weather-and-loop.js(配線)

【仕様】
1. レッドストーン鉱石: y<16に生成、鉄ツルハシ以上、ドロップ=ダスト4-5、掘るとXP。
2. 部品: ダスト(地面に敷く。設置は solid 上面のみ、信号0-15減衰)、レッドストーントーチ(常時15、
   付いたブロックが動力を受けると消灯=NOT)、レバー(トグル)、石のボタン(1秒パルス)、
   感圧板(上にプレイヤー/ドロップ/モブで作動)、レッドストーンランプ(受電で発光、C5のlightLevel連動)。
3. 伝播: 新規 47-redstone.js に信号グラフを実装。ダスト/部品の設置破壊・入力変化でイベント駆動の
   再計算(毎フレーム全走査禁止)。tick=0.1s。ダストの見た目は十字の薄い model+信号強度で赤の明度変化
   (メッシュ再構築はイベント時のみ)。
4. 出力先: 木のドア/トラップドア/フェンスゲート(受電で開、断で閉。手動開閉と競合したら受電優先)、
   ランプ、TNT(受電で着火)。
5. ピストン・リピーター・コンパレーターは実装しない(明記)。

【受け入れ条件】
- レバー→ダスト15ブロック→ドアが開く。16ブロック目では届かない
- ボタンでTNT着火、感圧板で自動ドア、トーチでNOT回路が組める
- ランプが夜の照明として機能する(ライトエンジン連動)
- アイドル時のコスト0(イベント駆動)、セーブ/リロードで回路状態が復元

【テスト方法】__mcDbg に signalAt(x,y,z) を追加。上記の受け入れ回路を実際に組んでスクショ。

【禁止事項】共通前提に同じ。ピストン/リピーター禁止(スコープ膨張防止)。
```

### C16: ネザー

```
【目的】ゴールB の中核。黒曜石ポータル→ネザー地形→ブレイズ→醸造(ポーション)までの片道分。

【対象ファイル】32-world-window.js / 22-block-types.js / 20-textures.js / 39-hostile-mobs.js /
53-inventory.js / 55-crafting.js / 52-raycast.js / 33-save-state.js / 14-sky.js / 82-weather-and-loop.js /
新規 35-dimensions.js(番号は32-36の空きに合わせて調整可)

【仕様】
1. ポータル: 黒曜石の縦枠(内側2x3)を火打石と打ち金で着火→内側に紫のポータルブロック
   (発光・非solid・アニメテクスチャ)。枠の検証は着火時のみ。プレイヤーが2秒立つと転移
   (暗転フェード→座標変換 1/8 →ネザー側に帰還ポータルを自動生成)。
2. ディメンション切替: DIM='overworld'|'nether' のグローバル状態。地形関数(columnDesc/terrainBlockAt)
   をDIMで分岐し、チャンクキャッシュのキーとセーブのedits/liquidsキーに DIM を含める。
   切替時は regenWindow 全再生成+空/霧/BGM切替(赤黒い霧、天井付き地形なので空は見えない)。
   ネザー地形: y=0-127相当、ネザーラック主体、溶岩海(y<31)、グロウストーン天井、
   ソウルサンド帯(移動減速)、ネザー水晶鉱石(→ネザー水晶)。水は置けない(蒸発音)。
3. ネザー要塞(ミニ): 既存の構造物ビルダー方式でネザーレンガの回廊+塔をまばらに生成。
   ブレイズスポナー的な「湧き部屋」(部屋内は常時ブレイズ上限2)。宝箱。
4. ブレイズ: 浮遊+火の弾(スケルトンの矢AIを流用、着弾で炎上)。ドロップ=ブレイズロッド。
5. 醸造: 醸造台(ブレイズロッド+丸石3)。UI=水入り瓶3+素材1。瓶=ガラス3。
   ネザーウォート(要塞に生成、ソウルサンドに植えられる)→奇妙なポーション→
   ・+ブレイズパウダー=力(攻撃+3, 3分) ・+マグマクリーム省略 ・+火打石…簡略化:
   力/俊敏(速度+20%)/治癒(即時HP4)/耐火(溶岩無効2分)の4種でよい。効果は SURVIVAL に
   effects 配列を追加しHUDにアイコン+残り時間。
6. ゾンビピッグマン等は実装しない。ガストは任意(できれば: 浮遊+火球+泣き声)。

【受け入れ条件】
- ポータル築造→着火→転移→帰還の往復ができ、座標が1/8対応、リロード後も両側のポータルが残る
- ネザーで採掘/建築/戦闘が通常どおり動き、オーバーワールドのセーブと混ざらない
- ブレイズを倒してロッド入手→醸造台で耐火ポーション→溶岩を泳いで無事(実測)
- ネザーにいる間、オーバーワールドの液体シム/モブが止まっている(リソース分離)

【テスト方法】__mcDbg に dim()/goNether() を追加。往復・醸造・効果の全チェーンを実測しスクショ。

【禁止事項】共通前提に同じ。ワールドウィンドウ機構の複製(2世界同時保持)は禁止。切替方式で実装。
```

### C17: エンド + エンダードラゴン

```
【目的】ゲームクリアを実装する。エンダーアイ→地下要塞→エンドポータル→ドラゴン戦→クリア。

【対象ファイル】C16と同様のディメンション機構 / 39-hostile-mobs.js / 32-world-window.js /
53-inventory.js / 55-crafting.js / 59-progress.js / 新規UI(クリア画面)

【仕様】
1. エンダーアイ: パール+ブレイズパウダー(ロッド→パウダー2)。使用すると要塞方向へ飛んで消える
   (方向演出+効果音)。要塞は既存の地下遺跡ビルダーを拡張した「エンドポータル部屋つき要塞」を
   ワールドに数カ所生成(F6系のデバッグテレポートも追加)。
2. エンドポータル: 12個の枠にエンダーアイをはめる(要塞生成時に0-2個は既にはまっている)。
   完成で中央が黒い星空テクスチャのポータル化→乗るとエンドへ。
3. エンド地形: 浮島(エンドストーン)、黒曜石の柱6-10本、柱頂にエンドクリスタル(攻撃で爆発)。
   空は黒+星。帰りはドラゴン討伐後の帰還ポータルのみ。
4. ドラゴン戦: HP200。飛行パス(柱の外周旋回⇄中央ポータルへの急襲)。クリスタルが残っている間は
   近くのクリスタルからビームで回復(2/s)。攻撃=体当たり+ノックバック大。弓が主戦力になる調整。
   討伐でXP大量+ドラゴンの卵(記念ブロック)+帰還ポータル出現。
5. クリア画面: 討伐時に「終わりの詩」風の全画面テキスト(スキップ可)→オーバーワールドへ帰還。
   進捗(59-progress.js)に「エンドへ」「ドラゴン討伐」を追加。2周目はドラゴン不在でよい。

【受け入れ条件】
- アイで要塞を探し、ポータルを完成させ、エンドへ行き、クリスタルを割り、ドラゴンを倒し、
  クリア画面を見て、帰還する——全チェーンが通しでプレイ可能(実測必須)
- 死亡してもエンドポータルから再挑戦できる(進行が破綻しない)
- クリア後もワールドで遊び続けられる

【テスト方法】__mcDbg に goEnd()/dragonInfo() を追加。弱体化フック(HP10)で通し確認後、
本番HPでバランス確認。

【禁止事項】共通前提に同じ。シュルカー/エンドシティは実装しない。
```

### C18: ワールド選択/作成画面

```
【目的】本家のタイトル→ワールド選択→作成の流れを付け、複数ワールドを管理できるようにする。

【対象ファイル】index.html / src/main.js / 54-input.js(overlay) / 33-save-state.js /
31-settings.js / 新規 05-title-screen.js 相当(番号は既存構成に合わせる)

【仕様】
1. 起動時のオーバーレイを「タイトル画面」に拡張: ワールド一覧(名前/シード/最終プレイ日時/
   プレイ時間)、新規作成(名前+シード入力またはランダム)、削除(確認ダイアログ2段階)、開始。
2. ワールドのメタ情報を localStorage `mc_worlds`(配列)に持ち、既存の `mc_save_<seed>` /
   `mc_edits_<seed>` / IndexedDBキャッシュはそのまま利用。既存セーブがある場合は初回起動時に
   一覧へ自動移行(名前=「ワールド1」)。
3. ゲーム中 Esc のポーズ画面に「タイトルへ戻る」(セーブして一覧へ)を追加。
4. 文言・レイアウトは Sonnet の S1 と分担: 本タスクは機能とDOM骨格、装飾文言はSonnetが後で調整
   できるよう、テキストは1オブジェクトに集約しておく。

【受け入れ条件】
- ワールドを2つ作り、それぞれの編集/インベントリ/位置が独立して保存・復元される
- 削除で該当キーとメッシュキャッシュが消える(他ワールドは無傷)
- 既存プレイヤーのセーブが消えずに一覧へ移行される(最重要・回帰テスト必須)

【テスト方法】localStorage/IndexedDB のキーを開発者ツールで確認しながら 作成→切替→削除。
既存セーブがある状態を再現してから移行を確認。

【禁止事項】共通前提に同じ。既存セーブキーの改名・削除ロジックの安易な実装禁止
(削除は明示的なユーザー操作のみ)。
```

---

## 4. Sonnetに渡す指示（コピペ用）

> Sonnet にも §1【共通前提】を貼る（特に音楽会場と生成物に触らない点）。実装は DOM/CSS/文言まで、
> ゲームロジックの変更は禁止と明記してある。

### S1: タイトル/ポーズ/ヘルプ画面の文言とUI

```
【目的】ゲームの「外装」を本家らしく整える。タイトル画面・ポーズ画面・操作ヘルプの文言と
軽量なDOM/CSSを担当する。

【対象ファイル】index.html / src/game/styles.css / C18が集約した文言オブジェクト
(実装済みなら該当ファイル、未実装なら overlay 部分)

【仕様】
1. タイトル画面: ゲーム名(例:「MINECRAFT WEB — 和風ワールド」等、ユーザーに確認)、
   スプラッシュ的な一言、操作の要約(WASD/マウス/E/Tab/Q/Shift/Ctrl/数字/F3系デバッグ)。
2. ポーズ画面: 再開/設定/(あれば)タイトルへ戻る、操作一覧の全表。
3. 初回プレイ向けチュートリアルトースト文言10個程度(例:「木を殴って丸太を集めよう」
  「E でインベントリ。2x2で作業台が作れる」…進行に沿った順)。表示ロジックは実装済みの
   進捗HUD(59-progress.js)の目標文を流用してよい。文言のみ差し替え。
4. トーンは日本語・です/ます無しの本家風簡潔文。ゲームロジックのJSは変更禁止。
   文言はすべて1つのオブジェクト/セクションに集約し、開発者が差し替えやすくする。

【受け入れ条件】タイトル/ポーズ/ヘルプが実画面で崩れず表示され、npm.cmd run check が通り、
console error/warn ゼロ。全キー表記が実装と一致していること(54-input.js を読んで裏取りする)。

【禁止事項】ゲームロジック・キーバインド・音楽会場関連(R T Y U I O P [ Z X C V J K L M)の
文言変更は現状維持で記載のみ。innerHTML への未サニタイズ挿入禁止。
```

### S2: README全面改稿

```
【目的】READMEを「遊べるゲームの顔」にする。現状のREADMEを読み、実装済み機能を正確に反映した
全面改稿を行う。

【対象ファイル】README.md のみ

【仕様】
1. 構成: ゲーム概要 → 遊び方URL(GitHub Pages) → 操作一覧(完全な表) → 主な機能
   (サバイバル/クラフト/農業/戦闘/ネザー/エンド…実装済みのものだけ) → クラフト早見表(主要20個) →
   開発(セットアップ/npmコマンド/ファイル構成の概要) → 既知の制限。
2. 機能の裏取り: HANDOFF.md と MINECRAFT_GAP_PLAN.md を読み、「実装済み」とされているものだけを
   載せる。未実装機能を書かない。
3. 操作表は 54-input.js を読んで全キーを網羅(音楽会場キーも「おまけ: 音楽会場」節として記載)。

【受け入れ条件】記載された全機能・全キーが実装と一致(サンプル10個を実プレビューで確認)。
単独コミットにせず、次の機能コミットに同梱してもらうこと(プロジェクトのコミット方針)。

【禁止事項】コードの変更禁止。憶測で機能を書かない。
```

### S3: 進捗ツリーの拡張文言

```
【目的】新機能(XP/エンチャント/ネザー/エンド等)に合わせて進捗(実績)を約40段に拡張する
「文言と順序」を設計する。

【対象ファイル】src/game/parts/59-progress.js(構造を読み、進捗定義の配列に追記する。
達成判定のロジックが必要な項目は「判定条件案」をコメントで書き、実装はCodex担当に委ねる)

【仕様】本家の進捗ツリー(石器時代→はじまりのモンスター退治→ダイヤモンド!→ネザーへ→
目には目を→エンドへ→解放)の流れを参考に、日本語で目標文(20字以内)+達成文(30字以内)を設計。
既存21段の文言も一貫したトーンに揃える。

【受け入れ条件】既存の達成判定が壊れないこと(判定条件を変えない。文言のみ変更+新項目追加)。
npm.cmd run check 成功。

【禁止事項】達成判定ロジックの変更・追加実装(コメント提案まで)。
```

### S4: ゲーム内文言の総監修

```
【目的】アイテム名・ブロック名・死亡メッセージ・UI細部の日本語を本家の公式日本語訳のトーンに
統一する。

【対象ファイル】53-inventory.js(ITEM_DEFSのname)/ 22-block-types.js(TYPESのname)/
51-survival.js(死亡画面)/ 56-inventory-panel.js(UIラベル)/ 38-travelers.js(取引文言)

【仕様】
1. 本家日本語版の呼称に合わせる(例: 丸石/石炭/作業台/かまど→「かまど」は本家では「かまど」、
   エンチャントテーブル→「エンチャントテーブル」等)。変更は name 文字列のみ。
2. 死亡メッセージを死因別に(落下/溶岩/溺れ/爆発/モブ別)。死因情報が無ければ実装提案をコメントで。
3. 取引/ヒント文言の誤字・トーン統一。

【受け入れ条件】name 文字列と表示文言以外のコード変更ゼロ(diffで確認)。クラフトのkeysや
アイテムidは変更禁止(セーブ互換)。npm.cmd run check 成功。

【禁止事項】id/キー/判定値の変更。音楽会場文言の変更。
```

---

## 5. 最終レビュー観点（ユーザー用チェックリスト）

各タスクの受け入れ条件に加え、**毎回**以下を確認する:

1. **回帰スモーク**: 新規ワールド→木を掘る→作業台→ツルハシ→採掘→設置→夜に敵と戦闘→死亡→
   リスポーン→リロードで全復元。5分でできる一連。
2. **会場無傷**: `git diff --stat` に 60-rave-venues.js / 70-music.js / music/ が**出ていない**こと。
   R キーで会場が起動し音が鳴ること。
3. **セーブ互換**: タスク前のセーブデータでリロードしてエラーが出ない・持ち物が消えない。
4. **性能**: ロードが数秒以内、F3飛行で目立つカクつきがない、5分放置でFPSが落ちない
   (液体・レッドストーン導入後は特に)。
5. **console**: error/warn ゼロ。
6. **セキュリティ**: 看板・ワールド名などユーザー入力の表示が textContent/Canvas 経由か
   (innerHTML 直挿入がないか diff で確認)。localStorage キーの削除コードが「明示操作時のみ」か。
7. **HANDOFF.md 更新**: 実装内容・触ったファイル・新 __mcDbg フック・次の候補が追記されているか。
8. **見た目の判定**(C1/C4-C6/C16/C17): スクショを本家の画像と並べ、名前を伏せても
   「Minecraftのそれ」に見えるか。

---

## 6. 実行順序（推奨）

```
第1週(見た目の革命):  C4 → C5 → C6 → C7   ← 最優先。これだけで別物になる
並行可(依存なし):     C1, C2, C3          ← ライト作業の合間に消化
第2週(世界が動く):    C8 → C9
第3週(進行ループ):    C10 → C11 → C12 → C13 → C14
第4週(ゲームの完成):  C16 → C17           ← ここでゲームとして「クリア可能」になる
仕上げ:               C15(レッドストーン) → C18(ワールド管理)
Sonnet:               S1(C18後かつ随時) / S4(C14後) / S3(C17後) / S2(最後)
```

- **依存の要点**: C5→C7、C8→C9、C10→C11、C12(パール)+C16(ロッド)→C17。それ以外は入れ替え可。
- 時間が尽きる場合の優先順位: **C4-C6(ライト) > C8-C9(水) > C10-C11(XP) > C16-C17(次元) > 残り**。
  ライトと水だけでも「本家に近い」体感が最も大きく変わる。
- 各タスク完了ごとに push し、GitHub Pages で実機確認してから次へ進む。

---

## 7. 本家仕様 数値リファレンス（各手順書の末尾に同梱済み）

> 本家Java版準拠の実数値。**§3の本文と数値が食い違う場合はこの節を優先**する。
> 当ゲームに存在しない要素に依存する箇所は「簡略」「代用」と明記してある。

#### R-C4

```
- 光レベルは0〜15の整数。輝度変換は本家近似: brightness(L) = 0.8^(15-L)（L15=1.0、L14=0.8、
  L0≈0.035）。完全な黒にはせず最低0.035程度のアンビエントを残す。
- スカイライト: 空に直通する列は15。不透明ブロックで0。水と葉は1ブロック通過ごとに-1、
  ガラス等その他の透過ブロックは減衰なし。横方向はBFSで1ブロックごとに-1。
- 夜: 月明かりにより地上の実効明るさは「光レベル4相当」が下限。夕暮れ/明け方は
  昼(15相当)⇄夜(4相当)を時間で補間する。
- 正しい見た目の基準: 昼でも窓のない部屋の中は真っ暗。窓を1つ開けると窓の周囲だけ
  グラデーションで明るくなる。洞窟は入口から奥へ15,14,13...と1ブロックずつ暗くなる。
```

#### R-C5

```
- 光源ブロックの光レベル表（本家準拠）:
  松明14 / ランタン15 / 提灯14(独自) / 発光結晶13(独自) / 溶岩15 / かまど(点火)13 /
  グロウストーン15(C16で追加) / ネザーポータル11(C16) / レッドストーントーチ7(C15) /
  レッドストーンランプ15(C15) / エンチャントテーブル7(C11) / 醸造台1(C16) / 火15(あれば)
- 伝播: 全6方向へ1ブロックごとに-1。不透明ブロックで遮断。水中も伝播する(追加減衰なし)。
- 最終輝度 = max(スカイライト × 昼夜係数, ブロックライト) を 0.8^(15-L) で輝度化。
  ブロックライトは昼夜の影響を受けない(松明は昼も夜も同じ明るさで、夜に目立つだけ)。
```

#### R-C7

```
- 敵対Mobの湧き条件(本家1.18以降準拠): 湧き先セルのブロックライト=0 が必須。
  スカイライトが当たる場所(地上)はさらに夜のみ。屋内・洞窟はブロックライト0なら昼夜問わず湧く。
- 本家の距離定数(参考値。既存バランス定数を維持してよいが、寄せるならこの値):
  プレイヤーから24ブロック以内は湧かない / 128ブロック超で即デスポーン /
  32ブロック超は時々ランダムデスポーン。
- 日光燃焼: ゾンビ/スケルトンは「昼」かつ「頭上に不透明ブロックがない(スカイライト15直下)」で
  炎上。水中にいる間は燃えない。
- クモは夜湧きだが、光レベル12以上の場所では中立(先に攻撃しない)。
```

#### R-C1

```
- 階段の設置(本家): 水平向きはプレイヤーの視線方位（プレイヤーから見て奥が高くなる向き）。
  クリックした面の下半分に置けば通常、上半分なら逆さ階段(天井向き)。
  逆さ階段まで実装するなら素材ごと8ID。工数が厳しければ通常4IDのみとし「逆さは未実装」と
  HANDOFFに明記。
- 硬さ: 木の階段/ハーフ=2.0(斧適正) / 丸石系=2.0(ツルハシ) / 石レンガ系=1.5(ツルハシ)。
- ハーフブロック(下付き)の上に立つと高さ+0.5。ハーフの上面には通常ブロックを設置できること。
- 同一セルに上下ハーフを重ねて完全ブロック化(二重スラブ)は実装しない(明記)。
- 角の自動接続(内曲がり/外曲がり階段)は実装しない(明記)。
```

#### R-C2

```
- はしご: 登り2.35ブロック/秒、自然滑降は最大3.0ブロック/秒、スニーク中は完全停止。
  はしごに触れている間、落下距離は常にリセット。設置は「背面が不透明ブロックの側面」のみ。
  背面ブロックが壊されたら、はしごもアイテム化して落ちる。
- レシピ(本家): 棒7のH字 → はしご3 / ガラス6(横2段) → 板ガラス16 / 板材6+棒1 → 看板3。
- 看板: 4行×各15文字。設置直後に編集UI表示。壊すと看板1をドロップしテキスト削除。
- 板ガラス: 壊してもドロップなし(ガラス本体と同じ)。
```

#### R-C3

```
- 本家の速度: 歩行4.317/s、スニーク=歩行の30%(1.295/s)、スプリント=歩行の1.3倍(5.612/s)。
  ※当ゲームは既存 WALK/SPRINT 定数を維持する方針のため、スニークは WALK×0.30 で計算する。
- スニーク: 目線を約0.3下げる。onGround中、移動先の足元(直下1.0以内)に支えがない水平移動は
  打ち消す(縁から落ちない)。
- スプリント: FOV×1.15。空腹6(ゲージ3個)以下では開始できない。
- 消耗(exhaustion)システム(本家準拠): 内部値が4.0に達するたびリセットし、
  saturation>0ならsaturation-1、なければ空腹-1。
  加算表: スプリント0.1/m / ジャンプ0.05 / スプリントジャンプ0.2 / 攻撃命中0.1 /
  被ダメージ0.1 / ブロック破壊0.005 / 水泳0.01/m / 自然回復1HPごとに6.0。
- 自然回復: 空腹18以上で4秒ごとに1HP。空腹20かつsaturation>0なら0.5秒ごとに1HP(高速回復)。
- 飢餓: 空腹0で4秒ごとに1ダメージ、HP1で停止(本家ノーマル難易度準拠。餓死はしない)。
```

#### R-C8

```
- 水: 更新間隔0.25秒。平地で水源から7ブロックまで(レベル1〜7)。
- 溶岩(地上世界): 更新1.5秒。3ブロックまでしか流れない(レベルが2ずつ減る)。
  ネザー内では更新0.5秒・7ブロック(C16で切替)。
- 流れ先の選択(本家の重要挙動): 水平拡散の際、4ブロック以内に「下へ落ちられる穴」があれば
  その方向へ優先して流れる(これで水路が作れる)。実装が困難なら全方向拡散でも可だが、
  可能な限り実装する。
- 変換則: 溶岩[源]に水が触れる → 黒曜石 / 溶岩[流れ]に水が触れる → 丸石 /
  水[源]の上に溶岩が流れ込む → 石。
- 無限水源: 同じ高さの水源2つと水平に隣接し、下がsolidまたは水源のセルは水源になる。
- バケツで汲めるのは[源]のみ。[流れ]は汲めない。
- アイテムドロップは水に浮き、プレイヤー/Mob/アイテムは流れ方向に約1.4/sで押される。
```

#### R-C9

```
- 酸素: 15秒ぶん(バブル10個、1.5秒で1個減)。尽きた後は1秒ごとに2ダメージ。
  頭が水面から出たら約1秒で全回復。
- 水中移動: 水平は歩行の約50%、上昇(Space)約2.0/s、沈降は最大-2.2/s(ゆっくり)。
- 落下ダメージ: どんな高さからでも水に着水すれば0(深さ1ブロックでも)。
- 採掘速度: 頭が水没していると5倍遅い。さらに足が接地していないと追加で5倍(計25倍遅い)。
- 視界: 水中は青い霧(#3F76E4系)で視程を大きく狭める。頭が出た瞬間に即解除。
```

#### R-C10

```
- レベル必要XP(本家式): Lv0-15: 2×Lv+7 / Lv16-30: 5×Lv-38 / Lv31+: 9×Lv-158。
- 採掘XP: 石炭鉱石1-2 / ダイヤ鉱石3-7 / レッドストーン鉱石1-5(C15) / ネザー水晶2-5(C16)。
  鉄・金鉱石は採掘では0(精錬時に入る)。ブロック設置→再採掘でXPが無限に出ないよう、
  プレイヤーが設置したブロック(edits)からはXPを出さない。
- 精錬XP(取り出し時に個数ぶん): 鉄0.7 / 金1.0 / 食料0.35 / 丸石→石0.1 / 砂→ガラス0.1。
  端数は内部で蓄積し、整数になったぶんをオーブ化。
- 討伐XP: ゾンビ/スケルトン/クリーパー/クモ=5 / スライム大4・中2・小1 / エンダーマン5 /
  ブレイズ10。繁殖成立で1-7。
- 死亡時: 本家は「7×レベル(上限100)を散布して残り消滅」だが、このゲームは**キープインベントリ方針
  (死んでもアイテム・XPをロストしない)**のため死亡ロストは実装しない(2026-07-12 ユーザー決定)。
- 黒曜石: 硬さ50。ダイヤツルハシで約9.4秒。ダイヤ未満では壊せてもドロップしない。
- 火打石と打ち金: 鉄インゴット1+火打石1、耐久64。
```

#### R-C11

```
- 本棚: テーブルの周囲2ブロック(同じ高さ〜+1)に最大15冊で効果最大。
  0冊=最上段の要求レベル約8 / 15冊=最上段の要求レベル30(冊数で線形に上げる)。
- 3択スロット: 表示された要求レベル以上のとき選択でき、消費レベルはスロット位置ぶん(1/2/3)。
  (本家1.8以降の方式。要求レベルと消費レベルは別物であることに注意)
- 効果の数値(本家準拠):
  効率N: 採掘速度に N²+1 を加算 / 耐久N: 耐久消費が発生する確率を 100/(N+1)% に /
  ダメージ増加N: +0.5×N+0.5 / 防護N: 被ダメージ-4%×N(装備合計の上限64%) /
  火属性N: 命中時 4×N 秒炎上 / 射撃ダメージ増加N: 矢ダメージ×(1+0.25×(N+1)) /
  無限: 矢を消費しない(矢を1本以上所持している必要あり)。
- 金床: 合成後の耐久 = 両者の残耐久合計 + 上限の12%(上限まで)。
```

#### R-C12

```
- クモ: HP16 / 攻撃2 / 移動は歩行Mobの約1.3倍速 / 体型は平たい(高さ0.9・幅1.4の判定)。
  光レベル12以上では中立(攻撃されたら反撃)。壁登り可。
  ドロップ: 糸0-2 + 33%でクモの目(食料: 空腹2・低回復の非常食として扱う)。
- エンダーマン: HP40 / 攻撃7 / 高さ約2.9。プレイヤーのカーソルが頭部に合うと敵対(遠距離でも)。
  水・雨に触れると0.5秒ごとに1ダメージ+ランダムテレポート。テレポート範囲は32ブロック以内。
  ドロップ: エンダーパール0-1(C17で必須になるので、敵対させて倒せば確実に集まる調整に)。
- ドロップ追加: 牛/鹿=革0-2 / 羊=生羊肉1-2 / ゾンビ=腐肉0-2 / スケルトン=骨0-2+矢0-2。
- 骨粉: 骨1→骨粉3。小麦に使うと残り成長時間の40〜100%(ランダム)を短縮。苗木は45%で即成長判定。
  使用時に緑のキラキラパーティクル。
```

#### R-C13

```
- 食料表(空腹回復/隠し満腹度saturation)(本家準拠):
  リンゴ4/2.4 / パン5/6.0 / ステーキ8/12.8 / 焼き豚8/12.8 / 焼き鳥6/7.2 / 焼き羊肉6/9.6 /
  生牛肉3/1.8 / 生豚肉3/1.8 / 生鶏肉2/1.2 / 生羊肉2/1.2 / 腐肉4/0.8 / ベリー2/0.4 /
  金リンゴ4/9.6+吸収ハート4(2分)。
- saturationの上限は「現在の空腹値」。消耗はまずsaturationから減り、0になってから空腹が減る。
- 精錬時間は既存と同じ(1個10秒)。生肉はかまど燃料では焼けない…ではなく通常の精錬素材として扱う。
- 金リンゴ: 金インゴット8+リンゴ1(リンゴを中央に囲む)。吸収ハートは黄色で表示し、
  被弾時に通常HPより先に減り、2分で消える。
```

#### R-C14

```
- ハサミ: 鉄インゴット2(斜め配置)、耐久238。羊に使うと羊毛1-3(倒すと1)。刈られた羊は
  約90秒で羊毛回復(本家は草を食べて回復だが簡略でよい)。
- サトウキビ: 水に隣接する砂/土/草の上に自生。3段まで成長。一番下を壊すと全段ドロップ。
  紙=サトウキビ3(横一列)→3枚 / 本=紙3+革1 / 本棚=板材6+本3(上段・下段が板材、中段が本3)。
  本棚を壊すと本3がドロップ(板材は返らない)。
- 砂利: シャベル適正。壊すと10%で火打石、90%で砂利。地形生成では川底・洞窟にまばらに追加。
- 金ツール: 採掘速度倍率12(ダイヤの8より速い)/ 耐久32 / 採掘可能ティアは木ツール相当 /
  金の剣=攻撃4。
- 氷: 雪原の水面に生成。ツルハシ適正。壊すとドロップなしで水に戻る(下が空気なら消えるだけ)。
  上を歩くと滑る(摩擦0.98相当で慣性が残る)。
- 羊毛: 硬さ0.8。白+染色2〜3色(赤=花、黄=花。3色まで)。ベッド=羊毛3+板材3。
```

#### R-C15

```
- レッドストーン鉱石: y<16に生成 / 鉄ツルハシ以上 / ドロップ=ダスト4-5 / 採掘XP1-5。
- 信号: ダストは強度15から1ブロックごとに-1。レッドストーントーチ=出力15・光レベル7・
  取り付いたブロックが受電されると消灯(NOT)。反転は0.1秒(1RSティック)遅れる。
- ボタン: 石=押して1.0秒間ON(木を作るなら1.5秒)。レバー=トグル。
  感圧板: 石=プレイヤー/Mobで作動、木=ドロップアイテムでも作動。出力はどちらも15。
- ランプ: 受電で光レベル15(C5のライトエンジンに接続)。
- レシピ: RSトーチ=棒1+ダスト1 / レバー=棒1+丸石1 / 石のボタン=石1 / 感圧板=同素材2(横並び) /
  ランプ=グロウストーン1+ダスト4(C16前は発光結晶1+ダスト4で代用、C16後に本家レシピへ移行)。
```

#### R-C16

```
- ポータル: 黒曜石で内側2×3の枠(角は不要、最小10個)。火打石と打ち金で着火。
  中に立って4秒で転移(※本文の2秒は4秒に置き換え)。座標対応: ネザー座標=地上の1/8。
  ポータルブロックは光レベル11・紫パーティクル・独特の環境音。
- ネザー地形: ネザーラック(硬さ0.4・ツルハシで一瞬) / 溶岩海(下層) / ソウルサンド(移動40%減速+
  わずかに沈む) / グロウストーン(天井の房。壊すとダスト2-4、ダスト4→グロウストーンブロック) /
  ネザー水晶鉱石(鉄ツルハシ、XP2-5)。水は設置不可(蒸発の煙と音)。
  【本家の名物】ネザーでベッドを使うと爆発する(威力5、TNT超)。必ず入れること。
- 溶岩はネザー内では更新0.5秒・7ブロックまで流れる(C8の定数を切替)。
- ブレイズ: HP20 / 浮遊 / 火の弾3連射→クールダウン(弾: 直撃5+5秒炎上) / 溶岩・火ダメージ無効 /
  ドロップ=ブレイズロッド0-1(プレイヤー撃破時のみ)。
- ガスト(任意): HP10 / 大型・浮遊 / 火球は着弾で小爆発 / 泣き声の環境音。
- 醸造: 醸造台=ブレイズロッド1+丸石3 / ガラス瓶=ガラス3のV字→3本 / 1回で瓶3本同時、20秒 /
  燃料=ブレイズパウダー1で20回ぶん(簡略可) / 水入り瓶+ネザーウォート→奇妙なポーション →
  +ブレイズパウダー(ロッド1→2)=力(攻撃+3、3分) / +砂糖(サトウキビ1→1)=俊敏(+20%、3分) /
  +金インゴット=治癒(即時4HP)(本家はキラキラスイカだが未実装のため代用) /
  +ネザー水晶=耐火(2分)(本家はマグマクリームだが未実装のため代用)。
- ネザーウォート: ソウルサンドにのみ植えられる。4段階成長、骨粉無効。要塞内に初期生成。
```

#### R-C17

```
- エンダーアイ: エンダーパール1+ブレイズパウダー1。右クリックで最寄りの要塞方向へ浮上しつつ
  飛んで消える(数秒後にその場にドロップ80% / 消滅20%)。
- 要塞: 地下深くに生成。ポータル部屋に12個の枠があり、各枠は10%の確率で最初からアイ入り。
  12個すべて埋めると起動し、中央3×3が「エンドポータル」(黒い星空テクスチャ)になる。
- エンド: エンドストーンの浮島 / 黒曜石の柱6〜10本(高さはばらばら) / 柱頂にエンドクリスタル
  (攻撃1回で爆発・威力6。近くにいると大ダメージ)。
- ドラゴン: HP200。行動パターン: 外周旋回(矢でしか届かない)→時々中央へ急降下体当たり
  (ダメージ10+特大ノックバック)→中央ポータル上でホバリング(このとき近接攻撃のチャンス)。
  クリスタルが1本でも残っていると毎秒2HP回復(クリスタルから回復ビームの演出)。
  つまり攻略手順は本家どおり「先に全クリスタルを壊す→ドラゴン本体」になる。
- 討伐: XP12000(オーブ分割で大量に散布) / 中央に帰還ポータル起動 / ドラゴンの卵(記念ブロック)。
  「終わりの詩」風スタッフロール(スキップ可)→オーバーワールドへ帰還。再訪時ドラゴンはいない。
```

